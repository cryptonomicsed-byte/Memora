{
  "name": "@sigil/mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP tool surface for the Sigil launchpad: agents build, review, promote and roll back content-addressed dApp front-ends",
  "type": "module",
  "bin": {
    "sigil-mcp": "./src/index.ts"
  },
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "start": "node src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "@mysten/sui": "^2.31.3",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2"
  }
}
